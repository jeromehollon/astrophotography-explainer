# Design notes: interaction, state, and reconciliation log

Working document for the Figma phase. Companion to `docs/copy.md` (prose) and `docs/figma-state.json` (node and token ids). Sections 3–5 become the handoff.

Figma file: https://www.figma.com/design/jtYi1LQf1fVyUAUfAgthP5/Astrophotography-Explainer

## 1. Approved direction (2026-09-24)

- Light theme on cream (`surface/page` = cream/100 #F2EBE0); space-themed Bauhaus: geometric primitives, cobalt / red / ochre pulled toward space, violet for "outside the camera", ink rules. No gradients, no glow.
- Type: Jost (headings, SemiBold/Medium), Public Sans (body 16/24, lede 18/28, 680 px measure), IBM Plex Mono (every number the pipeline produces).
- Wizard: one hash route per page, browser back/forward work. Page order per SPEC §7: Welcome → Noise & Defects → Bias → Darks → Flats (2) → Alignment → Algorithms → Light Frames (2) → Workbench.
- Page skeleton: TopBar → reading (page head, full-width illustration, section 1 with an ROI image, further prose) → 6 px ink rule → dark "Try it" stage → BottomNav.
- Teaching rules: one idea per page; ROIs only until the workbench; calibration pages show raw / corrected / what-was-removed; nothing collapses; no page counters; no binning labels; no success signal.
- Voice: neutral narrator, conversational blend of enthusiastic expert and textbook; no pithy declaratives; Figma text is authoritative and the owner edits it directly.

## 2. Colour and shape semantics (never colour alone)

| Meaning | Token | Shape | Where it appears |
|---|---|---|---|
| Bias | `source/bias` cobalt | circle | badge, illustration, toggle label icon |
| Dark signal / hot pixels | `source/dark` red | square | badge, illustration |
| Flat-field response | `source/flat` ochre | triangle | badge, illustration, reference-frame star |
| Random noise | `source/noise` grey | diamond | badge, illustration |
| Outside the camera | `source/external` violet | pentagon | badge, frame notes |
| Light frame | `ink/shape` | 4-point star | badge |

## 3. Interaction and state-preservation notes

- **One store, two views.** Lesson pages and the workbench read and write the same state: `calibration` (bias, dark, darkFlat, flat level), `frames[]`, `reference`, `algorithm` + params, and the four ROI ids. Lesson pages show a subset of controls; controls a page does not show keep their current value. Entering the workbench never resets anything, with one exception (owner request, 2026-09-24): if the state matches no scenario exactly on arrival, the workbench applies the Default scenario so the selected button and the state agree.
- **Lesson pages lock what they do not teach.** E.g. Flats-1 forces `bias=false, dark=true`? — no: it applies whatever calibration the store holds *except* it forces `flat=50` on and off with the toggle and `darkFlat` with the second toggle. See §5 for the open question on defaults.
- **Stale vs pending.** Changing any input marks ROI tiles *Pending* until the worker returns (`ROITile State=Pending`, message "Result pending"). The full-image result is invalidated separately and shows "Result pending — press Stack".
- **Keyboard.** Tab order per page: top bar (brand, chapters, workbench) → reading-column links/callouts (none focusable) → stage controls in reading order (toggles, chip groups as one radio-group tab stop with arrow keys, sliders with arrow/PageUp/PageDown) → bottom nav. Focus ring: 3 px `border/strong` outside on cream; 3 px `accent/focus-on-stage` (ochre) outside on the stage. Toggle: Space. Chips: arrows. Scenario buttons: plain buttons.
- **Thumbnails enlarge on click (P1 Noise & Defects).** The tour table shows each example at 120×80; clicking opens the full crop (720×480, or 880×660 for the galaxy) in a plain overlay that closes on click or Escape. No hover affordance is drawn in the static design.
- **Full stack.** Button → ProgressBar (percent, frames done, Cancel keeps focus) → result view → export buttons. Cancel returns to the previous result if any.
- **Export.** FITS (float32, linear, header per SPEC §4.6) and PNG (8-bit, current stretch). Filename pattern `ngc7331_stack_<n>f_<method>_<calib>.<ext>`.

## 4. Placeholders in the design

| Where | Placeholder | Real source |
|---|---|---|
| ROITile image wells on P4 Flats-1 | real crops from `assets/flats-1/` (script `tools/assets/flats_page1.py`) | `/api/roi` crops at runtime; the Figma crops define the stretch (STF target bg 0.30, clip −1.8 MADN) |
| ROITile image wells and the light-frame patch on P3 Darks | real 64×64 crop at 5× from `assets/darks/` (script `tools/assets/darks_page.py`, region x 106, y 852 of f03) | `/api/roi` crop at runtime with nearest-neighbour enlargement; same STF |
| P3 master-dark piece (376×252 native pixels, own AutoSTF) and amp-glow image (old-camera dark, binned 2×2, AutoSTF 0.30 / −2.8) | `assets/darks/dark_piece.png`, `ampglow_full.png` | Stage D static art |
| P3 hot-pixel ring marker on the Raw and Computed tiles | ochre ellipse at the pixel in `stats.json` `circled_hot_pixel` | drawn by the section from the same coordinates |
| ROITile image wells on P2 Bias | real crops from `assets/bias/` (script `tools/assets/bias_page.py`); the shared STF is computed on the bias-subtracted crop so the raw tile reads as lifted rather than the corrected tile clipping to black | `/api/roi` crops at runtime |
| P2 full master bias | `assets/bias/bias_full.png` (every 5th pixel, no averaging, linear stretch 158–164 DN) | Stage D static art; must subsample, not bin, or the grain disappears |
| P2 average-brightness bars | drawn rectangles sized from `assets/bias/stats.json` (raw 1,233, computed 1,073, bias 161 in the ROI) | live ROI means from the pipeline |
| P0 Welcome image wells | real full-field images from `assets/welcome/` (script `tools/assets/welcome_page.py`): f07 sub, WBPP master, colour composite | Stage D static art |
| Every other ROITile image well | "PLACEHOLDER · real ROI crop" | `/api/roi` crops, reference-frame STF (SPEC §4.5) |
| P1 Noise & Defects image wells (single-frame and master problem crops, 8 tour crops) | real crops from `assets/noise/` (script `tools/assets/noise_page.py`, numbers in `stats.json`) | Stage D static art; the two problem crops are a 2200×1650 region binned ×5, the flat and cloud crops are whole-field ×8 and quarter-field ×4, the rest native pixels |
| P4 full-field flat and before/after frame | `assets/flats-1/flat_full.png`, `frame_before.png`, `frame_after.png` (binned ×5) | Stage D static art |
| P7 Algorithms image wells | real crops from `assets/algorithms/` (script `tools/assets/algorithms_page.py`, numbers in `stats.json`): the satellite ROI of frame 3, the four inputs, the four-frame average and median, and the fifteen-frame stack (f00–f14) with each of the five methods, all a 2304 px crop at bin 4 on one STF (target bg 0.30, clip −1.8 MADN) computed on the frame-3 crop | pipeline ROI at a per-lesson bin of 4 (see §5 item 18) |
| P7 one-pixel box | text values from `assets/algorithms/stats.json` → `one_pixel` (985 / 908 / 903 / 909, average 926.25, median 908.5) | live values from the pipeline at the same ROI pixel |
| P7 worked-example boxes (kappa-sigma, winsorized, RCR) | text values from `stats.json` → `fifteen_frames.one_pixel_traces`: the fifteen brightnesses of one trail pixel (903, 913, 908, 985, 903, 909, 901, 918, 904, 916, 905, 905, 915, 920, 907) and each method's middle, spread, limits, rejections and result, from scalar versions of the same algorithms | live per-pixel traces from the pipeline, if the workbench ever exposes them |
| P7 worked-example boxes (kappa-sigma, winsorized, RCR) | text values from `stats.json` → `fifteen_frames.one_pixel_traces`: the fifteen brightnesses of one trail pixel (903, 913, 908, 985, 903, 909, 901, 918, 904, 916, 905, 905, 915, 920, 907) and each method's middle, spread, limits, rejections and result, from scalar versions of the same algorithms | live per-pixel traces from the pipeline, if the workbench ever exposes them |
| Histogram and Histogram/Mini | "PLACEHOLDER" bar shape | `histograms.json` |
| P5 Flats-2 image wells | real crops from `assets/flats-2/` (script `tools/assets/flats_page2.py`): 85 % ROI in the problem slot, three full-field master flats on one shared linear stretch, stage tiles at the Flats-1 crop and STF (target bg 0.30, clip −1.8 MADN) | `/api/roi` crops at runtime; the full-field flats are Stage D static art |
| P5 histogram chart | vectors drawn from `assets/flats-2/histograms.json` (one raw flat per level, frame 0000, 256 bins over 0–65,535, real counts on a linear scale) | `histograms.json` for the flats, or the same asset file |
| FrameCard thumbnail | solid navy rect | bin-8 thumbnails |
| Flats-1 and Darks stages | none (readouts removed) | — |
| Copy numbers marked `[PLACEHOLDER: …]` in `docs/copy.md` | FWHM, star counts, background spreads, noise ratios | `stars.json`, `histograms.json`, pipeline |
| Illustration stars | drawn dots | none (illustrative, labelled so) |
| P8 Light Frames 2 and P9 Workbench frame list | FrameCard thumbnails are real bin-8 views from `assets/light-frames-review/frames/<id>_thumb.png` (script `tools/assets/light_frames_review_page.py`); the mini histogram is a drawn 96×40 frame placed over the right end of each card (`frame-list` › `histogram-<id>`, absolute), 96 bars over the full 0–65,535 range from `stats.json` → `frames[id].framecard.histogram_bars_px`; the library Histogram/Mini inside the card is hidden | bin-8 thumbnails and `histograms.json` (`histogram_full`) |
| P8 Light Frames 2 frame viewer and ROI tiles; P9 light-frames block | real crops from `assets/light-frames-review/` (frame 0003 whole field at bin 4, its four ROIs at bin 2, the wide galaxy stack at bin 2, average and median) on one STF (target bg 0.30, clip −1.8 MADN, computed on the reference frame's wide galaxy region) | `/api/roi` at bin 4 (whole frame) and bin 2 (ROIs); the pipeline's stack for the selected frames and method |
| P9 ROI strip | real median stacks of the 20 raw frames on the four ROIs (`stack_<roi>_median.png`) | live pipeline ROIs at bin 2 |
| P9 calibration and flat tables | master thumbnails from `assets/workbench/cal_<id>.png` (script `tools/assets/workbench_page.py`, bin 4; bias, dark and dark flat on AutoSTF 0.30 / −2.8; the flats with no stretch at all, owner direction); histograms are drawn rectangles from `assets/workbench/stats.json` (128 bins over the full 0–65,535 range, heights scaled to 100 px) | Stage D static art; `histograms.json` for the masters |
| P9 state B progress values ("35 % · 7 of 20 frames") | illustrative `[PLACEHOLDER: live progress]` | the worker pool's progress events |
| P6 Alignment image wells | real crops from `assets/alignment/` (script `tools/assets/alignment_page.py`, numbers in `stats.json`): f05, f02, f02 aligned onto f05, and both averages (`problem_naive.png` is generated but no longer placed) | `/api/roi` crops and the pipeline's own warp at runtime; the Figma crops define the stretch (STF target bg 0.30, clip −1.8 MADN, computed on the f05 crop) |
| P6 Alignment illustration | drawn chart (√N and 1/√N curves, theoretical) | none; it is the illustration for this lesson |
| P8 Light frames image wells | real images from `assets/light-frames/` (script `tools/assets/light_frames_page.py`, numbers in `stats.json`): the three cloud copies of frames 14–16 whole field (binned ×8, one STF computed on the frame-14 cloud copy, target bg 0.30, clip −1.8 MADN), frame 11 and its tracking-error copy on one 1128×752 galaxy crop shown at 564×376 (STF on the frame-11 crop), and a 48 px star from both frames enlarged ×8 | Stage D static art; all calibrated with the app default (dark, 50 % flat with dark flats) |
| P8 FWHM drawing | bars drawn from `stats.json` → `fwhm_star.*.profile_dn` (one image row through one real star, sky subtracted, 1000 DN = 200 px); half-peak line and the ochre width band are computed from the same numbers | none; it is the illustration for this lesson |

## 5. Proposed spec changes and items to reconcile

1. **P4 Flats-1 layout (owner direction, 2026-09-24).** Reading column: intro on the optical path → the real master flat, full field → "Reading the flat as a correction map" with a one-pixel side panel (600 ÷ 0.75 = 800) → whole-image before/after → why the flat needs dark flats. So this page does show two full-field views (the flat, and the frame before/after); ROIs-only still applies to the Try-it tiles. Tiles are **Raw / Computed / What was removed**; every tile is populated in every toggle state and carries an owner-written caption. Experiment 2's tiles are the same light-frame ROI and use the 50 % flat (a 10 % variant was tried on 2026-09-24 and rejected; the owner kept 50 % as the Figma frames show): Raw = divided by the flat as shot, Computed = divided by the dark-flat-calibrated flat, removed = their difference (about 0.4 DN at the mote core; the raw noise cancels because both tiles divide the same frame, so on its own symmetric stretch it shows a clean donut much like experiment 1's; `stats.json` has the numbers). The optical-path SVG and its prose share a cream bounding box with the real flat to its right; the before/after pair sits in a dark box. The "bias only" flat variant (`flat_50_bias`) is not exposed on this page. The "bias only" flat variant (`flat_50_bias`) is not exposed on this page; it remains available in the workbench.
2. **"What was removed" view.** Lesson stages show `computed − raw` centred on mid grey (lighter = the correction added brightness, darker = it removed some); the tile shows that difference in every toggle state; only the Computed tile follows the switch. This is a display-side computation not in `StackResult`; the pipeline or the section needs a difference helper. SPEC P4 already lists a difference view as a candidate.
3. **Under- vs over-correction with an uncalibrated flat.** SPEC P4 says over-correction toward the corners; with a dark-subtracted light and the flat's own offset left in, the expected effect is under-correction (the map is flatter than the truth). Verify the sign with `flat_check.json` before the caption ships.
4. **Top bar shows sub-steps** (4 in Calibration, 2 in Light frames). The shell needs the section registry to expose chapter → pages so markers can be drawn. The page counter was removed on purpose.
5. **Lesson ROI tiles carry no readout and no bin tag.** SPEC §4.5's noise readout stays a workbench feature (NoiseReadout component) and, per copy, appears in the Alignment lesson under the average tile. Decide whether Noise & Defects shows a number (SPEC P1 AC expects a readout falling as 1/√N).
6. **FrameCard columns** are checkbox · number · thumbnail · histogram · FWHM · note. Star count is not shown in the list; it is quoted in the Light Frames prose. If the owner wants it back, add a column.
7. **Illustrations are full width (1200×360)** above the first section; a 440×330 ROI image slot sits beside "The problem you can see" on every lesson page. Stage D (static art) should produce those ROI crops at 2× for the illustration slot.
8. **Amp-glow example** on Darks (display-only old-camera dark). Resolved by the owner on 2026-09-24: shown binned 2×2, full field, in a static dark box in the reading column, like the Flats-1 before/after. The Try-it stays ROI-only.
10. **P3 Darks: bias is not discussed (owner direction, 2026-09-24).** SPEC P3 and §6.1 call for the page to show the bias as "already inside the dark" when the dark is on. The owner ruled that the Bias lesson already covered it, so the Darks page never mentions the bias and applies it to every light-frame image (Raw = light − bias; Computed = light − master dark, which carries the bias). The "already inside the dark" wording remains a workbench matter. The page also has a single toggle (Dark) rather than the none → bias only → dark sequence in SPEC P3.
11. **P3 Darks: temperature mismatch is not mentioned (owner direction, 2026-09-24).** SPEC P3 says the page notes that the dark was taken colder than the lights. The owner asked to omit it; the page only says that darks must match the lights' exposure time and temperature so the heat signal accumulates at the same rate. Consequence for the SPEC P3 acceptance criterion ("visible hot pixels disappear only with the dark applied"): with the master dark as supplied it holds only for a handful of pixels. The master dark carries a median 37% of each hot pixel's excess in the lights (`assets/darks/stats.json`, `dark_over_light_excess_ratio_percentiles`), so after subtraction the typical hot pixel still sits about 11 standard deviations above the background; only 19 hot pixels in the whole frame fall back into the noise. The owner chose to trust the master dark as is and asked for a region that shows removal as well as possible, so the region is chosen around one of those pixels (it is circled on the tiles) and the other hot pixels in it visibly dim. The Computed caption in state B quotes the real numbers. Dark optimization (scaling the dark, a WBPP option) was offered and declined.
13. **P3 Darks has no "What was removed" tile (owner direction, 2026-09-24).** The Try-it shows Raw and Computed only; the learner compares them by eye. This differs from the calibration-page rule in §1 and from Flats-1, and is the owner's call for this page.
12. **P3 Darks: pixel patches are 320×320, not the 440×330 slot.** The owner asked for a 64×64 native region shown pixel for pixel, so both patches are enlarged 5× to 320×320 (integer factor, no resampling blur) and the ROITile image wells are overridden to 318 px square. Runtime: the ROI endpoint must serve this region at native resolution and the tile must draw it with nearest-neighbour scaling (`image-rendering: pixelated`).
9. **P2 Bias replaces SPEC P2's histogram, sample-pixel readout and SVG peel-away (owner direction, 2026-09-24).** The Try-it stage is a Raw / Computed ROI pair (no "What was removed" tile: the owner judged the darkening of the Computed tile clear enough, so the three-tile rule has this one exception) with one Bias toggle and a two-bar chart of the average pixel brightness in the ROI (raw vs computed, bias share marked as a cobalt segment with a circle marker). No histogram, no per-pixel readout, and the illustration is static. The lesson holds `dark=false` so the bias is actually applied (SPEC §6.1 drops it when a dark is on); that rule is taught on Darks. The master bias is shown whole-field in the reading column (owner asked to see the grain), a further exception to "ROIs only". SPEC P2's AC ("histogram shift equals the master bias median within 1 ADU") should become "the drop in ROI average equals the ROI mean of the master bias".
10. **Prototype flow across Figma pages.** Prototype links cannot cross pages, so the final flow is assembled on the "Prototype" page by moving one frame per app page there and wiring Next/Previous. Page agents build on their own pages first.

10. **P0 Welcome has no "Try it" stage, no ROI tiles and no drawn illustration (owner direction, 2026-09-24).** The page skeleton in §1 lists a 1200×360 illustration and a dark stage on every lesson page; Welcome is the exception to both. Its illustration slot holds a real triptych (one 300 s sub f07, the WBPP master, the colour composite) in a dark box, and the rest of the reading column is two two-column rows: observatory prose (680) beside a cream side panel with the hook prose (440), then the galaxies prose (472) beside the colour composite (680). BottomNav has a single "Start: Noise & Defects →" button (Previous hidden). SPEC P0 stands; the skeleton rule needs the exceptions written down. Full-field views are allowed on Welcome per §5.1.
11. **Welcome prose does not define its terms.** SPEC §9 asks that light, stack, calibration and similar words be defined on first use. The owner's Welcome text uses "calibration frames", "stacking" and "stack" without definitions and is kept verbatim; the definitions land on Noise & Defects. Reconcile §9 or the Welcome copy.
12. **Deer Lick Group claim.** The Welcome prose says the four galaxies above NGC 7331 are gravitationally bound to it and will merge within 1–3 billion years. Published redshift distances place NGC 7335, 7336, 7337 and 7340 roughly ten times farther away, so they are a line-of-sight grouping. Kept verbatim at the owner's direction; flagged for a fact check before launch.
13. **Spelling on Welcome.** The owner's prose uses American spelling ("color"); captions and the illustration label on that page follow it ("Color photograph"). The rest of the file uses British spelling ("colour"). Pick one before the copy is final.

10. **P1 Noise & Defects is a reading-only introduction (owner direction, 2026-09-24).** No Try-it stage, no ink rule, no state variants. SPEC P1's interactive layer-selectable sensor SVG and the 1 → N averaging control with a 1/√N noise readout are not on the page; the illustration is the shared "Noise & Defects / Light path" component unchanged, and "why we stack" is explained in prose (four frames halve the random variation, sixteen quarter it). Item 5 above is therefore settled for this page: no readout. If the owner wants the averaging demo back, it belongs on the Alignment page (which already shows the √2 drop) or in the workbench.
11. **P1 survey vs "one idea per page".** The owner asked this page to tour every error source (random variation, readout offset, dark signal and hot pixels, uneven response, misalignment, satellites, clouds and tracking) with its fix, so it deliberately touches the topics of every later lesson. It is a compact table (thumbnail, badge and cause, what you see, what deals with it) with a 120×80 thumbnail per row that enlarges on click. The thumbnails are provisional: the owner may remove them and keep the text columns only. The whole-field flat and quarter-field cloud crops are static reading-column images, allowed under the ROIs-only rule's static-area exception.
12. **"Fix" lines in the P1 tour** are plain label text ("Removed with a dark frame."), not links or controls. They are set in label/md, text/primary.

14. **P5 Flats-2 layout and teaching choices (owner direction, 2026-09-24).** The illustration slot holds a histogram chart of the three raw flats instead of an optical-path drawing. The curves use the real pixel counts on a linear vertical scale (ticks at 2 and 4 million pixels; the 50 % flat is a low, wide hill and the 10 % and 85 % flats are spikes, which is itself the teaching point) on one linear brightness axis from black to the brightest value a pixel can report, with a grey band from the 85 % curve to the right edge labelled "Pixels are full: extra light spills over". The owner first asked for equal-height peaks and then for the real counts once they proved readable; both are in `assets/flats-2/histograms.json`. The full-well prose says the capacity is a property of the sensor as a whole, distinct from the brightest value a pixel can report (the end of the number scale). The full-well limit is kept vague on purpose (no number) and lives in a Note aside, not in the core prose; SPEC P5 wanted it as the core explanation. The chart reuses cobalt / ochre / red as series colours with a text label at every peak, so colour is not the only carrier, but those tokens also mean bias / flat / dark elsewhere; revisit if it confuses.
15. **P5 stage is a four-way chip group, not a toggle.** States: No flat (default, frame A), 10 %, 50 %, 85 %. The Computed tile and its caption follow the chips; the "What was removed" tile shows the difference for the chosen flat, and in the No-flat state it is uniform mid grey (`roi_removed_none.png`) because nothing was removed (owner direction, 2026-09-24). Unlike Flats-1, the removed tile therefore follows the chips too.
16. **P5 claim that the 10 % flat leaves a trace of the dust shadow.** `flat_check.json` gives a residual mote contrast of −0.54 % with the 10 % flat and +0.36 % with the 50 % flat, so the numbers are nearly equal and of opposite sign. The prose says a trace survives with the 10 % flat (owner direction: let the example show it). Check the tiles in the built app; if the difference is not visible, soften the sentence.
17. **P5 shows three full-field views** (the master flats) in a static reading-column box, the same exception to "ROIs only" as Flats-1's real flat and before/after pair.

14. **P6 Alignment is a reading-only page with a static first stack (owner direction, 2026-09-24).** No Try-it stage, no ink rule, no state variants, no align toggle and no noise readout; SPEC P6's interactive toggle and the "about a 1/√2 drop" readout are replaced by two equation rows in one dark box (frame 5 + frame 2 = average, first as they are and then after alignment) that the learner compares by eye. After the owner's review the page runs in one direction: why more exposures help (chart, prose, square-root aside), then what goes wrong when they are averaged as they are, then the fix. The 440×330 "problem" slot is gone: the owner asked for the problem image and the stacking rows to be one block. The illustration slot holds the SNR chart (panels titled "Random noise present in final image" and "Signal from galaxy in the image", owner's wording; the right panel plots √N) instead of a drawn scene.
15. **P6 frame pair.** The owner first chose f05 + f14, and asked that the meridian flip never be mentioned. f14 is an East-side frame, rotated about 180° relative to f05, so in the naive average each star's twin lands on the far side of the galaxy and the picture reads as "more stars", not as misalignment; the owner rejected it on review. The page now uses **f05 + f02**, SPEC P6's pair (about 49 px apart, the largest shift among the West-side frames), and every star shows as a close pair. The flip is still never mentioned.
16. **P6 aligned tiles are a design-time approximation.** The f02 warp in `tools/assets/alignment_page.py` uses the Stage A homographies (f05 ← f07 ← f02) with scipy cubic-spline interpolation on the crop; the pipeline will use Lanczos-3 (SPEC §6.4), so SPEC P6's AC "the aligned stack matches the pipeline golden output" cannot be checked until the pipeline exists. Calibration is the app default (dark, 50% flat with dark flats) and f02 is normalized onto f05 with the global scale + offset rule (§6.5) before averaging.
17. **P6 background-noise claim.** `assets/alignment/stats.json` records the spread of a 160 px star-free patch: by MADN the naive average and the aligned average both drop to about 0.72 and 0.68 of a single frame (the √2 rule applies to both, since averaging is averaging); the aligned one is slightly lower because the interpolation in the warp smooths f02 a little. The prose says the aligned average's sky is smoother than either single exposure, which the numbers support; it does not claim the naive average is noisier than the aligned one. No number is quoted on the page (owner: "allow the eye to see").
18. **P6 red circles.** The owner asked for red circles on the alignment stars. Red is `source/dark` in the colour table (§2); the circles use `red/600` directly and carry their meaning in the captions ("Alignment stars circled in red"), so colour is not the only carrier. If red should stay reserved for dark signal, switch the circles to ochre (`source/flat`, already "reference-frame star" in §2).
19. **P6 chapter number.** The eyebrow reads "Chapter 5 · Alignment", following Bias's "Chapter 4 · Calibration". Confirm the chapter numbering across pages before the copy is final.

18. **P7 Algorithms layout (owner direction, 2026-09-24).** The Try-it stage sits at the top of the page, directly under the page head, and replaces both the illustration and the 440×330 "problem" slot; there is no illustration on this page. The reading column follows the stage: a "What happens at one pixel" box, a five-method table (method, recommended frame count, what happens to an unusual value, when it is useful), three in-depth prose sections (kappa-sigma, winsorized, robust Chauvenet), and a closing comparison of the same satellite ROI stacked from the first fifteen raw frames f00–f14 (owner: fifteen frames, f03 among them) with each method, six 564 px tiles (Frame 0003 alone plus the five methods) in three rows of two inside a dark box; the earlier "select to enlarge" state was dropped once the tiles reached near full size. The skeleton rule in §1 needs this exception written down.
19. **P7 stack is four frames, not five.** SPEC P7 and the Satellite Trail Challenge use f03 + f02, f04, f05, f07; the owner asked for a four-frame stack, so the lesson uses f03 + f02, f04, f05. The scenario in the workbench keeps five.
20. **P7 ROI runs at bin 4, not bin 2.** The real trail is about 28 DN above a 42 DN noise per native pixel. At bin 1 or 2, frame 3 is not reliably the largest of four values at a trail pixel and the median only halves the trail (`stats.json` from an earlier run: 8.5 vs 10.5 DN at bin 2). At bin 4 the noise is 12 DN, the median leaves about 3 DN of a 28 DN trail, and the AC "gone with median" holds by eye. The 2304 px crop is centred on (4760, 2850) in frame-3 sensor coordinates. The pipeline needs a per-lesson bin (SPEC §4.5 already allows a per-lesson stretch; the bin should follow). No binning label is shown, per the lesson rules.
21. **P7 Average / Median control is a two-Chip group**, not a Toggle, because neither state is "off". Same pattern as Flats-2's four-way chips.
22. **P7 rejection numbers are honest but modest.** On the fifteen-frame stack, kappa-sigma rejects frame 3 at 17 % of trail pixels, winsorized at 33 %, RCR at 46 %; on the four-frame stack, 0 %, 8 % and 48 % (the twenty-frame numbers, 24 / 35 / 48 %, stay in `stats.json`). At the example pixel all three methods reject the 985 and give 909.1; kappa-sigma only manages it because fifteen values keep the outlier from swamping the spread (3.9 standard deviations against a limit of 3), which the box explains. The rest of the trail sits within about 3 standard deviations of the sky noise at each pixel. The prose says so rather than claiming the rejection methods remove the trail; if the owner wants a cleaner demonstration, the synthetic `*_TRAIL_*` frames in `light_synthetic_disaster/` carry a bright trail.
25. **P7 worked-example shading.** In the three example boxes the median value card is cream/300 with a 2 px ink outline and a rejected value card is `source/dark-soft` (red/200); a second or later rejection pass would use the same red at falling opacity. Red is the dark-signal colour in §2, so the meaning is carried by the legend row under the cards ("Median", "Rejected", or "Rejected, first pass" / "Rejected, pass N"), not by colour alone. In the current data every method rejects the 985 in its first pass and nothing later, so each legend reads Median / Rejected.
24. **P7 median remnant.** Frame 3 is the largest of the four values at about 86 % of trail pixels (66 % of fifteen), so the four-frame median leaves a faint trace; the Why callout sits inside the Try-it stage under the experiment (owner direction) and explains this with those numbers. The bin-4 figures in `stats.json` are the source.
25. **P7 worked-example shading.** In the three example boxes the median value card is cream/300 with a 2 px ink outline and a rejected value card is `source/dark-soft` (red/200); a second or later rejection pass would use the same red at falling opacity. Red is the dark-signal colour in §2, so the meaning is carried by the legend row under the cards ("Median", "Rejected", or "Rejected, first pass" / "Rejected, pass N"), not by colour alone. In the current data every method rejects the 985 in its first pass and nothing later, so each legend reads Median / Rejected.
24. **P7 median remnant.** Frame 3 is the largest of the four values at about 86 % of trail pixels (66 % of fifteen), so the four-frame median leaves a faint trace; the Why callout sits inside the Try-it stage under the experiment (owner direction) and explains this with those numbers. The bin-4 figures in `stats.json` are the source.
23. **P7 winsorized initial spread** uses 1.4826·MAD in place of the Rousseeuw–Croux Sn estimator that WBPP uses (same stand-in as `tools/precompute/integrate.py`). Kappa-sigma uses the population standard deviation. The browser pipeline should follow wbpp.md §4.6 exactly; the tiles may then differ slightly.

26. **P8 Light Frames 2 (Review the exposures) layout (owner direction, 2026-09-24).** The page has no illustration, no 440×330 problem slot, no ink rule and no dark Try-it stage; the controls live in the reading column: intro → "Your selected stack" (Average | Median chips and a 1,200 px wide galaxy view in a dark box) → "Examine the images" (two columns that grow as tall as they need, no scrollbars: 24 FrameCards on the left; Previous frame / Next frame, the whole frame at bin 4 and the four ROIs at bin 2 in a 2×2 grid on the right) → a teaser for the other methods and the Workbench button. The skeleton rule in §1 needs this exception. The lesson lives on its own Figma page, "P8 Light Frames 2", next to "P8 Light Frames" (same split as Flats / Flats 2); the page list in `figma-state.json` `plan.figmaPages` should gain it.
27. **P8 Light Frames 2 shows whole frames** (owner: "full resolution image, binned"), an exception to the ROIs-only rule. Frames are calibrated and normalized but not registered. Every image on P8-2 and P9 is oriented like the final master (owner direction, 2026-09-24): the master's reference is East-side, so West-side whole frames and every ROI crop and stack (registered onto the West-side frame 0003) are rotated 180°. The flip is never mentioned. The runtime needs a per-frame display rotation for the viewer and a fixed 180° rotation on ROI output while the reference is a West-side frame.
28. **P8 Light Frames 2 metrics.** Star count is omitted from the requirements (owner). FrameCard columns are unchanged (checkbox · number · thumbnail · histogram · FWHM · note). Altered copies keep their original's number and carry "Tracking error copy of 0011" / "Cloud copy of 0014" in the note column. Calibration is fixed and stated in one sentence of the lede (SPEC P8: "full calibration applied and locked").
29. **P8 Light Frames 2 ROIs.** In reference-frame (f03) sensor coordinates, 1,440×960 native each, shown at bin 2: satellite trail centred (4760, 2850); central galaxy (3246, 2100); galaxy group (3480, 2860); dust mote (760, 2397). SPEC P9 says the group is "just above" the galaxy, which is how it now shows, since every image is rotated to the master's orientation (item 27). The dust mote at bin 2 is faint (a 4.8 % dip); if it needs to read better, the mote ROI could be shown at native pixels like Flats-1. The wide galaxy view is 2,400×1,800 native at bin 2 (the Noise & Defects region, a little wider).
30. **P9 Workbench layout (owner direction, 2026-09-24).** Order: pinned ROI strip → Scenarios → Calibration (Bias, Dark, Dark flat) → Flat calibration (No flat, 10 %, 50 %, 85 %) → Light frames (the P8-2 block) → Combination method → Your master. SPEC P9's order (calibration → frames → algorithm → scenarios → reference view → full stack) is replaced. Each section header carries "Lesson: … →" links; the report rows link too.
31. **P9 ROI strip is fixed under the top bar** and stays visible while the page scrolls (owner). Owner request to add to the requirements: an enlarged view of a strip tile on hover. Tiles are 282 px (280×187 wells) and show the stack of the current selection with a caption naming the method and count.
32. **P9 drops "set as reference"** (owner). The reference is fixed to frame 0003 in the design. SPEC P9 item 2 should lose it.
33. **P9 has no algorithm parameters on the page.** SPEC §6.6's exposed parameters (too dark / too bright limits, RCR unlikelihood) are not in the owner's layout and were not built. Open question for the owner.
34. **P9 full stack and export.** SPEC P9 item 6 (Stack full image button, progress, cancel, result view, FITS or PNG export) becomes: a "Download PNG" button in the report; pressing it stacks the full image with a progress tracker (state B shows the ProgressBar and the button disabled) and then downloads the PNG. No FITS export and no result view are on the page (owner asked for a PNG). SPEC §4.6 and design-notes §3 "Export" should be reconciled.
35. **P9 scenarios.** Five plain Buttons (Default is Primary, the rest Secondary); the description under them shows the selected scenario and swaps to the hovered one while the pointer is over a button, returning when it leaves (owner). Descriptions come from the SPEC table; the pier side of the challenge frames is not mentioned.
36. **P9 selectors.** Calibration and flat rows use the FrameCard checkbox (cloned from the library component's `checkbox` frame, checked and unchecked variants) so the tables match the light-frame list (owner direction); the flat rows are therefore checkboxes even though only one flat can be on. The flat table has no note text and no lesson column; its lesson links float right of the section title. The method rows use Chip instances, the Flats-2 pattern. The bias row says it is already inside the dark and is used for the flats only when the dark flats are off (SPEC §6.1).
37. **Histograms are drawn, not the Histogram components, and every one spans the full 0–65,535 range (owner direction, 2026-09-24).** The components' bars cannot be repositioned inside an instance, so the workbench tables hold 128 drawn bars from `assets/workbench/stats.json`, and the FrameCard rows on P8-2 and P9 carry a drawn 96-bar strip over the card's right end. On the full range every light frame is a spike in the second bar (the sky sits near 1,000 of 65,535), so the 24 cards look alike; the cloud copies' wider spread and the sky level falling through the night are only visible on a zoomed axis (`stats.json` keeps the numbers). Hiding the nested Histogram/Mini instance removes it from the card's layout, so the FWHM column moves left by 108 px; the overlay is placed at x 452 to stay clear of it, and the list has a drawn header row (Frame, Image, FWHM, Note, Histogram) at the same x positions. The table histograms carry no axis label (owner: do not repeat the range on every row). Library request in `TODO.md`: a data-driven Histogram (or accept per-page drawings).
38. **Noise numbers for the stacks** are in `assets/light-frames-review/stats.json` → `stack_noise_madn_dn` (single frame about 22 DN; the 20-frame average about 5–7 DN; the median slightly noisier than the average). Neither page quotes them (no readouts on lesson tiles).

25. **P8 Light frames is a reading-only page (owner direction, 2026-09-24).** SPEC P8's "lighter workbench" (frame list with thumbnails, metrics and histograms, average/median, one galaxy ROI) is not on this page; the owner put the Try-it stage on the second Light-frames page ("Review the Exposures" in the TopBar), which is not built yet. This page ends with a teaser paragraph that carries the owner's Try-it instructions (Previous frame / Next frame, include or exclude, Average vs Median). No ink rule, no stage, no state variants: single frame A. SPEC P8's AC ("selection changes update the ROI in under 1 s") therefore belongs to the second page.
26. **P8 skips the top illustration slot and the "problem" slot.** By owner direction the 1200×360 slot under the page head is dropped (the lesson's drawing, the FWHM profile, sits in its own section after the tracking example, where the prose introduces it) and there is no "The problem you can see" 440×330 crop (the cloud examples need the whole field). The page shows whole-field views (three cloud copies, binned) and a galaxy crop in static dark boxes, the same exception as Flats-1 and Alignment.
27. **P8 prose deviations from the owner's draft.** The sentence about blinking between exposures was replaced by "Comparing one exposure with the next…" (owner: the reference to PixInsight's Blink process means nothing to a beginner). The satellite paragraph is text only (no crop of the trail). The FWHM section and the teaser wording are the agent's; the rest is the owner's draft verbatim, em dashes included.
28. **P8 FWHM numbers.** The prose quotes `stars.json`: frame 11 median FWHM 8.2 px and 179 stars, its tracking copy 8.6 px and 74 stars; every frame's median FWHM lies between 8.0 and 9.6 px ("between 8 and 10 pixels"). The drawn profile uses one real star at sensor (2941, 2292): FWHM 8.16 px in frame 11, and it is not detected at all in the tracking copy. Along one image row the trailed star is about as wide at half its own peak (8 px in both), but its peak is 616 against 995 DN, which is why the panel titles say only "Round star" and "Trailed star" and the prose carries the argument. The synthetic tracking copy alters only some of the stars (349 changed patches in the whole frame), so the profile star was chosen from the altered ones.
29. **P8 chapter number.** The eyebrow reads "Chapter 7 · Light frames"; the owner confirmed chapter 7 (2026-09-24). See item 19 for the other pages.
30. **Frame names are not zero-padded (owner decision, 2026-09-24).** Captions and prose say "Frame 14", "Frames 14–16", "Frame 3", not "Frame 0014"; the P8 text was changed accordingly and Alignment's "Frame 5" already follows it.
31. **P8 star cutout captions quote a direct fit.** The catalogue run finds no star at the tracking copy's position, so `stats.json` → `fwhm_star.direct_fit` fits a Moffat to the same 48 px cutout in both frames: 8.1 px in frame 11, and 11.7 px in the copy (17.0 px along the trail, 8.1 px across it). The captions quote those numbers; the app pipeline has no per-star fit, so they are static art.

## 6. Figma API gotchas recorded for other agents

- `figma.createAutoLayout()` frames get a default opaque white fill: set `fills = []` on every container.
- `resize()` on a TEXT node after `textAutoResize = 'HEIGHT'` resets it to `NONE`; set HEIGHT after resize.
- Font style strings in this file: Jost "SemiBold"; Public Sans "SemiBold"; Inter "Semi Bold" (with a space).
- Text nodes wired to a TEXT component property show the property default in instances; don't rely on per-variant characters for wired text.
- Concurrent `use_figma` calls are fine on different pages; same-page writes are sequential.
- Inside an instance, children cannot be moved (`x`/`y` throw "relative-transform cannot be overridden"); they can be resized, hidden, swapped and their fills changed. Bar-chart components therefore cannot be data-driven from an instance.
- `upload_assets` targets plain frames only; for wells nested in instances (`I…;…` ids) upload to a temporary frame and copy `fills[0].imageHash` across, then delete the temp.

## 6. Brand mark (owner decision, 2026-09-24)

The site mark is the "Registration" candidate: three copies of the NGC 7331 silhouette (an ellipse tilted 22°) offset like unaligned subs, with the cobalt copy on top and a cream core. Fills are red/400, ochre/300, cobalt/600 and cream/50. It lives in the library as `Logo/Mark` (64 px master, children scale) and `Logo/Favicon` (32 px) on "Library · Icons", with a 16/24/32 preview strip on cream and on the stage. Every TopBar variant now carries a 28 px instance in the brand slot in place of the earlier circle/square/triangle glyphs. The favicon is the same geometry: `assets/brand/favicon.svg`. The offsets blur at 16 px, so prefer 24 px or larger where possible.

## 7. Site title (owner decision, 2026-09-24)

The Welcome page head now reads "Astrophotography Calibration and Stacking, Explained." (Jost SemiBold 40, wraps to two lines at the 900 px measure) with a new `subtitle` line "Why every deep-sky photo starts with pictures of nothing." (Jost Medium 24/32, `text/secondary`) between the title and the lede. The lede paragraph is unchanged. The browser `<title>` should use the same title. The TopBar brand slot (every TopBar variant) now reads "Calibration & Stacking" (Jost Medium 20/22) over the subtext "Astrophotography explained" (Public Sans Regular 12/14, `text/secondary`), in a vertical `text` stack beside the 28 px mark. The brand frame hugs its content (236 × 36); the chapters still start at 424.
